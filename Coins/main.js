let curl = "https://api.currencyfreaks.com/latest?apikey=c01622797c5540799020aff4d4837990"fetch(curl)    .then((result) => {        // console.log(result);        let myData = result.json();        // console.log(myData);        return myData;    })    .then((currency) => {        let price = document.getElementById("price");        let Amreka = document.querySelector("#Amreka span");        let submit = document.getElementById("submit");        let sud = document.getElementById("SUD");        submit.addEventListener("click", () => {            let sudValue = sud.value;            Amreka.innerHTML = `( $${sudValue} )`;            for (let key in currency.rates) {                if (sudValue !== "" && typeof parseInt(sudValue) == "number") {                    let div = document.createElement("div");                    div.className = "egp";                    div.innerText = `Price In ${key} :: `;                    let span = document.createElement("span");                    let priceSUD = currency.rates[key] * parseInt(sudValue)                    span.innerText = priceSUD.toFixed(2);                    div.appendChild(span);                    price.appendChild(div);                }            }            sud.value = "";        })    });