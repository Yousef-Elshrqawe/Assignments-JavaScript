console.log("Assignment 1 -------------------------------");let start = 10;let end = 100;let exclude = 40;for (let i = start; i <= end; i += start) {    if (i === exclude) { // لو  الرقم  لا  يساوي  40 اطبعو       continue;    }    console.log(i)}console.log("Assignment 2 -------------------------------");let Start = 10;let End = 0;let stop = 3;for (let j = Start; j > End; j--) {    console.log((j < start ? End + "" + j : "" + j))    if (j === stop) {        break;    }}// طريقه  ثانيه//// let j = Start;//// for (; j >= stop; j--)// {//     if (j < Start)//     {//         console.log(`${End}${j}`);//     } else//     {//         console.log(j);//     }// }console.log("Assignment 3 -------------------------------");let sTart = 1;let eNd = 6;let breaker = 2;for (let g = sTart; g <= eNd; g++) {    console.log(g)    for (let b = breaker; b < eNd; b += breaker) {        console.log(` -- ${b}`)    }}console.log("Assignment 4 -------------------------------");let index = 10;let jump = 2;let enD = 0;let e = indexfor (; ;) {    if (e === jump) {        break;    }    console.log(e)    e -= jump}console.log("Assignment 5 -------------------------------");let friends = ["Ahmed", "Sayed", "Eman", "Mahmoud", "Ameer", "Osama", "Sameh"];let letter = "a";let s = letter.length / letter.length;let f = letter.length - letter.lengthfor (; f < friends.length; f++) {    if (friends[f][friends.length - friends.length].toLowerCase() !== letter) {        console.log(`${s} => ${friends[f]}`);        s++    }}// // طريقه  ثانيه// let i = friends.length - friends.length;// let j = friends.length / friends.length ;//// for (; i < friends.length; i++) {//     if (friends[i].startsWith(letter.toUpperCase()) === false) {//         console.log(`${j} => ${friends[i]}`);//         j++//     }// }console.log("Assignment 5 -------------------------------");let START = 0;let swappedName = "elZerO";// Output// "ELzERo"let bnb = swappedName.split("")for (; START < bnb.length; START++) {    if (bnb[START].toLowerCase() === bnb[START]) {        bnb[START] = bnb[START].toUpperCase();    } else if (bnb[START].toUpperCase() === bnb[START]) {        bnb[START] = bnb[START].toLowerCase();    }}console.log(bnb.join(""))console.log("Assignment 6 -------------------------------");let staRt = 0;let mix = [1, 2, 3, "A", "B", "C", 4];for (; staRt < mix.length; staRt++) {    if (typeof mix[staRt] !== "string") {        if (mix[staRt] === mix.length / mix.length){            continue;        }        console.log(mix[staRt]);    }}