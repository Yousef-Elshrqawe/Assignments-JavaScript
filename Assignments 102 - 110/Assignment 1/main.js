let number = prompt("Print Number From – To", "Example: 5-20").split("-");let one = +number[0]let    two = +number[1]let ul = document.querySelector("ul")if (one < two){    for (let i = one ; i <= two ; i++){        let li = document.createElement("li")        li.innerText = `=> ( ${i} )`       ul.appendChild(li)    }}else {    for (let i = one ; i >= two ; i--){        let li = document.createElement("li")        li.innerText = `=> ( ${i} )`        ul.appendChild(li)    }}