let createP = document.createElement("p");let createTxt = document.createTextNode("No Classes To Show");createP.appendChild(createTxt);document.querySelector(".classes-list > div").appendChild(createP);let inputRemove = "";document.querySelector(".classes-to-add").onblur = function () {    let  inputAdd = document.querySelector(".classes-to-add").value.toLowerCase().split(" ");    document.querySelector(".classes-to-add").value = "";    for (let i = 0; i < inputAdd.length; i++) {        inputAdd.sort()        if (inputAdd[i] === "") {            continue;        }        let createSpan = document.createElement("span");        let createTxt = document.createTextNode(`${inputAdd[i]}`);        createSpan.appendChild(createTxt);        createSpan.className = inputAdd[i];        document.querySelector(".classes-list > div").appendChild(createSpan);    }    if (document.querySelectorAll(".classes-list > div span").length > 0) {        document.querySelector(".classes-list div p").style.display = "none";    }};document.querySelector(".classes-to-remove").onblur = function () {    inputRemove = document.querySelector(".classes-to-remove").value;    document.querySelector(".classes-to-remove").value = "";    document.querySelector(".classes-to-remove").blur();    let createI = inputRemove.toLowerCase().split(" ");    for (let i = 0; i < createI.length; i++) {        let inpuR = document.querySelectorAll(`.${createI[i]}`);        for (let n = 0; n < inpuR.length; n++) {            inpuR[n].remove();        }    }    if (document.querySelectorAll(".classes-list > div span").length === 0) {        document.querySelector(".classes-list div p").style.display = "block";    }};